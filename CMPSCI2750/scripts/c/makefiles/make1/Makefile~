CC	= gcc		# The C compiler
CFLAGS	= -g -pg	# Enable debugging by default
TARGET	= math
OBJS	= main.o square.o cube.o

$(TARGET): $(OBJS)
	$(CC) -o $(TARGET) -pg $(OBJS)

$(OBJS): mymath.h

.PHONY: clean
clean:
	/bin/rm -f *.o *~ $(TARGET)
