function avg
{
    arr=$1;
    numarr=${#arr[*]}
    echo "Number of elements: $numarr"
    i=0
    while [ $i -lt $numarr ]
    do
	echo "Number $i: $arr[$i]"
	sum=$((sum + arr[$i]))
	echo $sum;
    done
    echo "Average is: $(sum / numarr)"
}
